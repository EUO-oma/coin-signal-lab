# 30분 개선 자동화 플랜 (실행형)

## 지금 반영됨
- 자동 새로고침(30초) + 카운트다운
- 미니 차트(최근 120봉) 시각화
- 시장 무드/온도계/브리핑
- Supabase 미적재 시 Binance 폴백

## 다음 자동화 (바로 실행 가능)
1. Binance->Supabase 적재를 작업 스케줄러에 등록 (5분마다)
2. 실패 로그 파일 남기기 (`logs/ingest.log`)
3. 3회 실패 시 Telegram 알림(선택)

## 품질 향상
- 멀티 타임프레임(15m/1h/4h) 동시 계산
- 지표 기여도 패널(RSI/MACD 각각 점수 기여)
- 신뢰도 점수(변동성 기반)

## 운영 체크리스트
- Supabase table: `price_candles` row count > 1000
- 대시보드 데이터소스가 `Supabase`로 표시되는지
- 카운트다운 정상 감소 및 30초마다 갱신
